# 01 — 卡片文字溢出修复

**What to build:** 窄宽度下面板与卡片中的长文字（面板标题、物品卡片名称、联合会员长标题、KPI 数值等）单行截断并显示省略号，完整内容经 `title` 悬浮提示可见，布局不再被撑破。修复落在共享组件层（Panel、Kpi）与页面级卡片标记两处，一次根治而非逐页打补丁。

**Blocked by:** None — can start immediately

**Status:** resolved

- [x] 面板头部标题超长时单行截断，完整内容可经悬浮提示查看
- [x] 物品网格卡片名称截断，状态标签、进度条、金额行不挤压错位
- [x] 联合会员面板长标题（名称 · 金额 · 服务区间）截断不破版
- [x] 共享 KPI 卡的数值与标签截断
- [x] 桌面各宽度（1024/1280/1440）下无横向滚动、无文字溢出
- [x] 响应式断点与移动端适配不在本 ticket（见 06）

## Answer

- 共享层：`Panel` 头部标题 `min-w-0 truncate` + `title` 提示、actions 包 `shrink-0` 容器；`Kpi` 标签/数值/sub 全截断（`src/components/te.tsx`）。
- 页面层：物品网格卡片、dashboard 待续费横幅/即将到期/红黑榜/回本进度卡、订阅与物品归档列表、渠道行名称、联合会员归档行——统一 `min-w-0 truncate` + 右侧操作区 `shrink-0`。
- 冒烟：1000px 宽五个页面无横向滚动；DOM 注入超长文本验证 Panel 标题截断；新建超长名物品验证卡片截断（已归档删除清理）。
- 表格未动（文字可换行不溢出，移动端横滚属 06）。
