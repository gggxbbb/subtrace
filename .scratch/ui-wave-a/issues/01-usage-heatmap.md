# 01 — 详情页逐日日历 → 压缩热力图

Type: task

**What to build:** `UsagePanel.tsx` 的逐日日历网格（~356 行 `grid-cols-7` 及 120-166 行 `calDays` 生成）替换为 GitHub 式压缩热力图：周为列（周一为首、北京墙钟）、7 行 × ≤53 列、小格、accent 色透明度 5 档深浅编码用量。时间窗固定为「今天」往前 53 周，与盈亏面板的周期导航解耦。语义按用量类型分裂：计数型 = 当日 Σ quantity；省钱型 = 当日已省金额；额度型 = 仅快照日有格、值为较上一快照消耗（REMAINING 取降量、USED 取增量；消耗 ≤0 按空白渲染、tooltip 如实显示差值）。交互仅 `title` 悬浮提示（日期 + 数值）。格子数值分桶与刻度计算抽为纯函数放 `src/lib/usage/` 下。

**Blocked by:** 无

**Status:** resolved

- [x] 热力图组件替换日历网格，面板编号/位置不变，其余面板不动
- [x] 三种用量类型语义分裂正确（求和 / 快照差值）
- [x] 53 列封顶，超出截断
- [x] 5 档分位刻度（空窗、单一值、均匀分布边界）
- [x] 分桶/刻度纯函数单测（周一为首、北京墙钟边界）
- [x] `calDays` 死代码移除
- [x] 移动端 375px 与桌面 1440px 冒烟：整年一屏可见
