# subtrace

个人开支追踪：周期性订阅（含联合会员、手动续费、活动价）与一次性物品的日均成本、用量盈亏。

## Language

**订阅 (Subscription)**:
一项持续付费的服务。通过付费记录与跟踪模式描述其费用与有效期。
_Avoid_: 会员服务、计划、plan

**付费记录 (Payment)**:
一次真实发生的付费：金额（可为空——只知道到期日不知道金额的存量订阅）、支付日期、以及这笔钱买到的服务区间（起、止日期）。实付金额可以与订阅标准价不同（活动价、手动续费）；可登记退款，成本按净额。金额未知的记录不产生费率。
_Avoid_: 账单、订单、扣费记录

**到期日 (Expiry Date)**:
订阅当前有效期的结束日，**排他**：到期日当天起不再覆盖（服务区间为 [起, 止)）。等于最后一笔付费记录的服务止期；周期模式下若无付费记录，则由锚定日期按周期推算。下一笔付费的服务起 = 上一笔止期即无缝顺延。
_Avoid_: 下次扣费日、续费日

**跟踪模式 (Tracking Mode)**:
订阅维护到期日的方式。**周期模式**：存锚定日期 + 周期 + 标准价，到期日推算为主、付费记录可修正锚点。**手动模式**：不维护周期，到期日完全由每次付费记录中填写的日期决定。
_Avoid_: 自动/手动订阅

**主币种 (Base Currency)**:
用户选定的计算与汇总货币。每条付费/物品记录在录入时同时固化原币金额与当时折算的主币种金额（快照），此后所有计算只读快照，改汇率不影响历史记录。

**汇率表 (Exchange Rate Table)**:
用户级的外币→主币种汇率集合，仅在录入记录时用于预填折算值（可手改）。每个币对可选自动（定时从汇率 API 更新）或手动（钉住固定值，适合长期持有外币）。

**受益实体 (Beneficiary)**:
从订阅获益的实体——**用户**（家庭共享）或**物品**（iCloud 之于多台设备）。订阅由付款人（所有者）单一持有并管理，受益方只读；成本按权重在查询时分摊，改权重全局重算。用量与盈亏按受益用户各自独立计算；物品的 TCO 包含其份额。

**用量 (Usage)**:
可量化订阅在一段服务区间内的使用或获益量。三种口径：**计数型**（逐条记录次数/时长，如健身房）；**额度型**（录入使用百分比或总额度+已用额度，如 coding plan 点数）；**省钱型**（记录折扣类会员省下的金额——京东 Plus、88VIP、盒马 X 等平台“已省”数据折算增量或逐笔自记，增量求和，ADR-0011）。计数/额度型配合替代单价计算盈亏；省钱型价值即金额本身，盈亏 = Σ已省 − 已摊成本，辅以回本差额。来源：手动录入，或用户编写的脚本定时从产品 API 同步（脚本仅额度型）。

**用量脚本 (Usage Script)**:
额度型订阅上由用户编写的 JS 脚本，经 cron 定时在沙箱（ADR-0007）中执行，从外部 API 拉取当前已用量并写入用量快照。仅 ADMIN 标记的信任用户可编写。

**任务 (Cron Job)**:
进程内调度注册表（ADR-0006）中的一个周期任务：系统任务（提醒扫描、汇率刷新）或用户的用量脚本。每次运行落一条**运行记录 (Job Run)**（起止、耗时、成败、消息），在任务大盘页可见。

**信任用户 (Trusted User)**:
由 ADMIN 在用户管理中勾选、获准编写用量脚本的用户。沙箱（node:vm）非强安全边界，信任标记是脚本功能的真实防线（ADR-0007）。

**替代单价 (Alternative Unit Price)**:
用户填写的“不买订阅、按次/按量付费”的单价（如健身房单次卡价、API 按量价）。计数/额度型盈亏 = 用量 × 替代单价 − 该区间已摊成本；省钱型无替代单价概念。

**每次实际成本 (Actual Cost Per Use)**:
已摊成本 ÷ 用量的纯描述性指标，随用量上升而下降，与盈亏并列展示。

**物品 (Purchase)**:
一次性买入的实物或资产：金额、购买日期、可选的预期寿命。采用回本模型摊销——预期寿命内按固定费率（金额/寿命天数），超期或未定寿命则摊至当天；卖出或报废时摊至实际截止日并扣除残值。
_Avoid_: 资产、装备

**追加费用事件 (Purchase Event)**:
物品买入后的一次性生命周期支出：配件、维修等（日期、金额、类型、备注）。金额计入物品净额，与买入价共用同一回本摊销窗口；维修可选延长预期寿命。周期性的后续费用（AppleCare、iCloud 等）不走事件，而是经受益实体机制把物品加为订阅的受益人（ADR-0003），成本独立成段，物品页以 TCO 汇总两块。

**残值 (Resale Value)**:
物品卖出或报废时回收的金额，摊销时从买入价中扣除。

**联合会员 (Bundle)**:
一笔打包付费购得的多个子会员组合。打包费按各子会员标准价比例分摊；每个子会员是一个完整的订阅（独立的计费周期、到期日、用量），可以是新建的，也可以是**已有订阅被关联**（向其追加 BUNDLE 付费记录，历史连续）。归属关系挂在付费记录上，同一订阅可多次参与联合会员。折扣类会员所含的折扣权益（如 88VIP 的淘宝折扣）也作为一个子会员——省钱型订阅，以手填分摊额获得成本基（ADR-0011）。
_Avoid_: 套餐、大礼包

**锚定日期 (Anchor Date)**:
周期模式下推算到期日的基准日。初始为订阅起始日；录入一笔带服务止期的付费记录后，锚定日期被改写为该止期。

**计费周期 (Billing Cycle)**:
周期模式下两次扣费之间的间隔。两种形态：**日历周期**（每 N 日/周/月/年，按日历推进，月/年锚定到原始日——31 号月付永远是 31 号）；**固定天数**（每 N 天）。推算误差由付费记录修正锚点吸收。

**标准价 (List Price)**:
周期模式下单个周期的常规价格（通常即自动扣费金额）。用于未记录付费时的成本估算，以及联合会员内部分摊的权重。
_Avoid_: 原价、定价
